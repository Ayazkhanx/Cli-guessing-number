{
  "name": "akx-cli-guessing-number",
  "version": "1.0.0",
  "description": "this is a simple CLI GUESING NUMBER GAME in which Computer will generate a random number and user will guess its number if guess number and random number get match user will win",
  "main": "index.js",
  "type": "module",
  "bin": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ayaz khanx",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^9.3.4"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7"
  }
}
